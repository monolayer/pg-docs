import{_ as a,c as t,a2 as o,o as r}from"./chunks/framework.BVrlcXZz.js";const m=JSON.parse('{"title":"Glossary","description":"","frontmatter":{"prev":false,"next":false},"headers":[],"relativePath":"guide/schema-definition/glossary.md","filePath":"guide/schema-definition/glossary.md"}'),n={name:"guide/schema-definition/glossary.md"};function s(i,e,l,c,h,d){return r(),t("div",null,e[0]||(e[0]=[o('<h1 id="glossary" tabindex="-1">Glossary <a class="header-anchor" href="#glossary" aria-label="Permalink to &quot;Glossary&quot;">​</a></h1><h2 id="check-constraint" tabindex="-1">Check constraint <a class="header-anchor" href="#check-constraint" aria-label="Permalink to &quot;Check constraint&quot;">​</a></h2><p>Type of <a href="#constraint">constraint</a> It allows you to specify that the value in a certain <a href="#column">column</a> or a group of columns must satisfy a Boolean (truth-value) expression.</p><h2 id="column" tabindex="-1">Column <a class="header-anchor" href="#column" aria-label="Permalink to &quot;Column&quot;">​</a></h2><p>An attribute with a certain name and data type in a <a href="#table">table</a> or view.</p><h2 id="constraint" tabindex="-1">Constraint <a class="header-anchor" href="#constraint" aria-label="Permalink to &quot;Constraint&quot;">​</a></h2><blockquote><p>A restriction on the values of data allowed within a <a href="#table">table</a>, or in attributes of a domain.</p></blockquote><p><em>Source</em>: <a href="https://www.postgresql.org/docs/current/glossary.html#GLOSSARY-CONSTRAINT" target="_blank" rel="noreferrer">PostgreSQL glossary</a></p><h2 id="database" tabindex="-1">Database <a class="header-anchor" href="#database" aria-label="Permalink to &quot;Database&quot;">​</a></h2><p>A database is a structure used to organize and store data. Specifically in PostgreSQL, a database is named collection of <a href="#sql-object">local SQL objects</a>.</p><h2 id="data-type" tabindex="-1">Data type <a class="header-anchor" href="#data-type" aria-label="Permalink to &quot;Data type&quot;">​</a></h2><p>A data type is the kind of data that can be stored in a table column or returned by a function. It specifies the format and limitations of the data, such as whether it’s a number, text, date, or something more complex like a JSON object. For example, the data type <code>ìnteger</code> is used for whole, and <code>character varying</code> is used for variable-length character strings.</p><h2 id="extension" tabindex="-1">Extension <a class="header-anchor" href="#extension" aria-label="Permalink to &quot;Extension&quot;">​</a></h2><blockquote><p>A software add-on package that can be installed on an instance to get extra features.</p></blockquote><p><em>Source</em>: <a href="https://www.postgresql.org/docs/current/glossary.html#GLOSSARY-DATABASE" target="_blank" rel="noreferrer">PostgreSQL glossary</a></p><h2 id="foreign-key" tabindex="-1">Foreign Key <a class="header-anchor" href="#foreign-key" aria-label="Permalink to &quot;Foreign Key&quot;">​</a></h2><blockquote><p>A type of constraint defined on one or more <a href="#column">columns</a> in a <a href="#table">table</a> which requires the value(s) in those columns to identify zero or one row in another (or, infrequently, the same) table.</p></blockquote><p><em>Source</em>: <a href="https://www.postgresql.org/docs/current/glossary.html#GLOSSARY-FOREIGN-KEY" target="_blank" rel="noreferrer">PostgreSQL glossary</a></p><h2 id="function" tabindex="-1">Function <a class="header-anchor" href="#function" aria-label="Permalink to &quot;Function&quot;">​</a></h2><p>Code that you can run within a database query. It can take inputs, return outputs, and always runs within one transaction. You can call a function as a part of a query, for example via SELECT. Some functions can return multiple rows of data, and these are known as <em>set-returning</em> functions.</p><p>Functions can also be used for <a href="#trigger">triggers</a> to invoke.</p><h2 id="index" tabindex="-1">Index <a class="header-anchor" href="#index" aria-label="Permalink to &quot;Index&quot;">​</a></h2><p>A <a href="#sql-object">SQL object</a> that contains data derived from a table or materialized view, allowing for fast retrieval and access of the original data.</p><h2 id="primary-key" tabindex="-1">Primary Key <a class="header-anchor" href="#primary-key" aria-label="Permalink to &quot;Primary Key&quot;">​</a></h2><p>A <a href="#constraint">constraint</a> that enforces values to be unique and <code>NOT NULL</code>. A primary key is used to reliably identify and distinguish between each individual record in a <a href="#table">table</a> and there can be only one primary key per table.</p><h2 id="schema" tabindex="-1">Schema <a class="header-anchor" href="#schema" aria-label="Permalink to &quot;Schema&quot;">​</a></h2><blockquote><p>A schema is a namespace for <a href="#sql-object">SQL objects</a>, which all reside in the same database. Each SQL object must reside in exactly one schema.</p><p>...</p><p>More generically, the term schema is used to mean all data descriptions (table definitions, constraints, comments, etc.) for a given database or subset thereof.</p></blockquote><p><em>Source</em>: <a href="https://www.postgresql.org/docs/current/glossary.html#GLOSSARY-SCHEMA" target="_blank" rel="noreferrer">PostgreSQL glossary</a></p><h2 id="session" tabindex="-1">Session <a class="header-anchor" href="#session" aria-label="Permalink to &quot;Session&quot;">​</a></h2><blockquote><p>A state that allows a client and a backend to interact, communicating over a connection.</p></blockquote><p><em>Source</em>: <a href="https://www.postgresql.org/docs/current/glossary.html#GLOSSARY-SESSION" target="_blank" rel="noreferrer">PostgreSQL glossary</a></p><h2 id="sql-object" tabindex="-1">SQL Object <a class="header-anchor" href="#sql-object" aria-label="Permalink to &quot;SQL Object&quot;">​</a></h2><p>In PostgreSQL, an “sql object” refers to anything you can create using a CREATE command, like <a href="#table">tables</a>, <a href="#function">functions</a>, or <a href="#function">data types</a>. Most of these objects are tied to a specific <a href="#database">database</a> and live within <a href="#schema">schemas</a>, which are like folders that help organize them. For example, tables and functions in the same schema must have unique names. Some objects, like <a href="#extension">extensions</a>, don’t live in schemas but still need unique names within their database. Additionally, there are global objects like roles, tablespaces, and databases that exist outside any single database and must have unique names across the entire database instance.</p><h2 id="table" tabindex="-1">Table <a class="header-anchor" href="#table" aria-label="Permalink to &quot;Table&quot;">​</a></h2><p>A collection of rows that share the same structure, where each row has the same set of columns in the same order and has the same name and data type across all rows.</p><h2 id="transaction" tabindex="-1">Transaction <a class="header-anchor" href="#transaction" aria-label="Permalink to &quot;Transaction&quot;">​</a></h2><p>A combination of commands that all succeed or all fail as a single unit. If a system failure occurs during the execution of a transaction, no partial results are visible after recovery. Transaction effects are not visible to other <a href="#session">sessions</a> until the transaction is complete, and possibly even later, depending on the isolation level.</p><h2 id="unique-constraint" tabindex="-1">Unique constraint <a class="header-anchor" href="#unique-constraint" aria-label="Permalink to &quot;Unique constraint&quot;">​</a></h2><p>Type of <a href="#constraint">constraint</a> that ensures that the data contained in a <a href="#column">column</a>, or a group of columns, is unique among all the rows in the <a href="#table">table</a>.</p><h2 id="trigger" tabindex="-1">Trigger <a class="header-anchor" href="#trigger" aria-label="Permalink to &quot;Trigger&quot;">​</a></h2><blockquote><p>A function which can be defined to execute whenever a certain operation (INSERT, UPDATE, DELETE, TRUNCATE) is applied to a relation. A trigger executes within the same transaction as the statement which invoked it, and if the function fails, then the invoking statement also fails.</p></blockquote><p><em>Source</em>: <a href="https://www.postgresql.org/docs/current/glossary.html#GLOSSARY-TRIGGER" target="_blank" rel="noreferrer">PostgreSQL glossary</a></p>',42)]))}const f=a(n,[["render",s]]);export{m as __pageData,f as default};
